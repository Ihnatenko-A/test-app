import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  root: {
    padding: '20px 0'
  },
  heading: {
    fontWeight: 'bold',
    marginBottom: 12,
  },
  locationBlock: {
    marginBottom: 40,
  }
}));
